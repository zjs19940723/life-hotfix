{
  "installCommand": "npm ci --include=optional --no-audit --no-fund --registry=https://registry.npmjs.org",
  "buildCommand": "npm run build",
  "outputDirectory": "./dist",
  "nodeVersion": "22.17.1"
}
